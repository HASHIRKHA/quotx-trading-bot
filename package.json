{
  "name": "quotx-trading-bot",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:api": "cd artifacts/api-server && pnpm run dev",
    "dev:ui": "cd artifacts/trading-terminal && pnpm run dev",
    "build:api": "cd artifacts/api-server && pnpm run build",
    "build:ui": "cd artifacts/trading-terminal && pnpm run build"
  }
}
