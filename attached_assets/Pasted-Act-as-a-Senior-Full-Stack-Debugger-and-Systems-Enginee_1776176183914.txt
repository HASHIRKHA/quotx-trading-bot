Act as a Senior Full-Stack Debugger and Systems Engineer. We need to perform a comprehensive stability pass to fix existing runtime errors while strictly maintaining all current 'Quantum' signal functionality.
1. Fix the Frontend Crash (TradingChart.tsx):
Locate line 109 in artifacts/trading-terminal/src/components/TradingChart.tsx.
The error historicalData.map is not a function indicates the component is receiving an object/error instead of an array.
Action: Implement a defensive check using if (!Array.isArray(historicalData)) return; before the .map() call. If the data is not an array, display a 'Loading Market Data' overlay instead of crashing [Log Data].
2. Backend API Hardening (market.ts):
In artifacts/api-server/src/routes/market.ts, refactor the historical data endpoint.
Action: Ensure this route always returns a valid JSON array [], even if the Alpha Vantage or Twelve Data APIs return a 429 rate-limit error or 500 server error. Log the error internally but never send it to the frontend chart component
.
3. Safe Mode & Entropy Recovery:
The bot is currently locked in Safe Mode (Entropy 0.998) because the synthetic data seeding creates too much randomness [Log Data, 144].
Action: Refactor signals.ts to ensure that once real Twelve Data REST history is successfully pulled and saved to the PostgreSQL database, the entropy calculation is completely re-run using only the real OHLC values. Safe Mode must auto-disengage if the real market trend is stable
.
4. Initial Price Synchronization:
Resolve the '----' price display by ensuring the REST Price Handshake is successful before the first WebSocket tick.
Action: If the initial REST quote fetch fails, implement a 1-second retry loop (max 3 tries) to ensure the UI has a starting price point immediately upon connection [Log Data].
5. Absolute Consistency Check:
Do not change any CSS glow effects, the purple dashed ghost candle logic, or the SVM/LSTM weights.
Verification: After applying fixes, confirm that both EUR/USD and BTC/USD load historical candles and stream live prices without any console errors [275, Log Data]."
